# Helm Charts Repository

This branch contains packaged Helm charts.

## Usage

```bash
helm repo add kube-node-ready https://imunhatep.github.io/kube-node-ready
helm repo update
helm search repo kube-node-ready
```
